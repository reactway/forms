import React, { useState } from "react";
import ReactDOM from "react-dom";
import { Form } from "@reactway/forms";

const App = (): JSX.Element => {
    return <div>
        <Form>

        </Form>
    </div>;
};

ReactDOM.render(<App />, document.getElementById("root"));
